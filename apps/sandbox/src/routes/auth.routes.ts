import { Router } from "express";

const router = Router();

router.post('/login', () => { });

router.post('/login/microsoft', () => { });

router.post('/logout', () => { });

router.get('/me', () => { });


export default router;