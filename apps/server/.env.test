# Test Environment Configuration
# This file is used when running tests with Jest

# Application
PORT=3001
NODE_ENV=test
CLIENT_URL=http://localhost:5173
LOGS_DIR=./logs/test
LOG_LEVEL=error

# Database (Use a separate test database)
DATABASE_URL=postgresql://test:test@localhost:5432/coesco_test?schema=public

# Redis
REDIS_URL=redis://localhost:6379/1
REDIS_HOST=localhost
REDIS_PORT=6379

# Email (Mock in tests)
SMTP_HOST=smtp.test.com
SMTP_PORT=587

# Auth & Security
JWT_SECRET=test-jwt-secret-do-not-use-in-production
JWT_EXPIRES_IN=1h
JWT_REFRESH_EXPIRES_IN=1d
API_KEYS=test-api-key-1,test-api-key-2
SOCKET_SECRET=test-socket-secret

# Azure AD (Mock values for testing)
AZURE_TENANT_ID=test-tenant-id
AZURE_CLIENT_ID=test-client-id
AZURE_CLIENT_SECRET=test-client-secret
AZURE_REDIRECT_URI=http://localhost:3001/auth/azure/callback

# AI/LLM (Mock for tests)
OPENAI_API_KEY=test-openai-key
ANTHROPIC_API_KEY=test-anthropic-key

# Jira (Mock for tests)
JIRA_API_TOKEN=test-jira-token
JIRA_EMAIL=test@example.com
JIRA_DOMAIN=test.atlassian.net
JIRA_PROJECT_KEY=TEST

# Fanuc Adapter
FANUC_ADAPTER_HOST=localhost
FANUC_ADAPTER_PORT=5001

# ODBC/ProSQL (Mock for tests)
ODBC_DRIVER={SQL Server}
PROSQL_USER=test
PROSQL_PASSWORD=test

# Legacy Databases (Mock for tests)
STD_HOST=localhost
STD_PORT=1433
STD_DB=test_std

JOB_HOST=localhost
JOB_PORT=1433
JOB_DB=test_job

QUOTE_HOST=localhost
QUOTE_PORT=1433
QUOTE_DB=test_quote

# Backup (Disabled in tests)
BACKUP_DIR=./backups/test
BACKUP_RETENTION_DAYS=1
BACKUP_ENABLED=false
