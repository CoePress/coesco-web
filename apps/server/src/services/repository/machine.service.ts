// This file was generated by generate-services.ts on 2025-09-04 11:33:35
import { Machine } from "@prisma/client";
import { BaseService } from "./_base.service";
import { prisma } from "@/utils/prisma";
import { BadRequestError } from "@/middleware/error.middleware";

type MachineAttributes = Omit<Machine, "id" | "createdAt" | "updatedAt">;

export class MachineService extends BaseService<Machine> {
	protected model = prisma.machine;
	protected entityName = "Machine";
	protected modelName = "machine";

	protected async validate(entity: MachineAttributes): Promise<void> {
		if (!entity.slug) throw new BadRequestError("slug is required");
		if (!entity.name) throw new BadRequestError("name is required");
	}
}
