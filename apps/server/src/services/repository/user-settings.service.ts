// This file was generated by generate-services.ts
import type { UserSettings } from "@prisma/client";

import { BadRequestError } from "@/middleware/error.middleware";
import { prisma } from "@/utils/prisma";

import { BaseService } from "./_base.service";

type UserSettingsAttributes = Omit<UserSettings, "id" | "createdAt" | "updatedAt">;

export class UserSettingsService extends BaseService<UserSettings> {
  protected model = prisma.userSettings;
  protected entityName = "UserSettings";
  protected modelName = "userSettings";

  protected async validate(entity: UserSettingsAttributes): Promise<void> {
    if (!entity.userId)
      throw new BadRequestError("userId is required");
    if (!entity.settings)
      throw new BadRequestError("settings is required");
  }
}
