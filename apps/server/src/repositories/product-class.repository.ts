// This file was generated by generate-services.ts
import type { ProductClass } from "@prisma/client";

import { BadRequestError } from "@/middleware/error.middleware";
import { prisma } from "@/utils/prisma";

import { BaseRepository } from "./_base.repository";

type ProductClassAttributes = Omit<ProductClass, "id" | "createdAt" | "updatedAt">;

export class ProductClassRepository extends BaseRepository<ProductClass> {
  protected model = prisma.productClass;
  protected entityName = "ProductClass";
  protected modelName = "productClass";

  protected async validate(entity: ProductClassAttributes): Promise<void> {
    if (!entity.code)
      throw new BadRequestError("code is required");
    if (!entity.name)
      throw new BadRequestError("name is required");
    if (!entity.depth)
      throw new BadRequestError("depth is required");
    if (!entity.isActive)
      throw new BadRequestError("isActive is required");
  }
}
